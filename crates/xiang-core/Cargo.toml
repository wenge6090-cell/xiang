[package]
name = "xiang-core"
version = "0.1.0"
edition = "2024"
description = "象语言核心类型：Gua 6-bit状态向量、八气算子、藏海、八卦"

[dependencies]
rand = "0.9"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
