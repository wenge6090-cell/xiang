# 象语言（XiangLang）— 三易统一约束算法语言规范

**版本：2.0** | **状态：规范完成**

> 象语言是易三卷的统一 AI 约束算法语言。  
> 它不是"中文编程"（用中文替代英文关键词），而是一种根植于中国传统象思维、面向 AI 系统的**确定性约束算法语言**。
>
> **归藏**定意识（心之律）· **连山**定策略（行之决）· **周易**定格栅（知之姿）  
> 三套独立算子体系，一套统一语法规范。三层各司其职，共同为 LLM 提供确定性认知骨架。
>
> → 哲学总纲见 [象.md](./象.md)  
> → 工程实现见 [CangVM](../crates/xiang-cangvm/)、[ShanVM](../crates/xiang-shanvm/)、[ZhouVM](../crates/xiang-zhouvm/)

---

## 〇、三易统一：象语言的三个维度

```
                   象语言（XiangLang）
               三易统一的约束算法语言
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
   归藏算子层       连山算子层       周易算子层
   八气状态机       思变策略引擎      八卦认知格栅
        │              │              │
   6-bit S 向量     六步决策链路     8状态Markov链
   藏海Hebbian      七种方位决策      五行生克权重
   元知觉偏离度     目标障碍图谱      认知姿态 + 温度
        │              │              │
    「心之律」       「行之决」       「知之姿」
    持续后台          每受阻触发       每轮任务
```

| 算子层 | 易卷 | 核心算符 | 状态空间 | 输出 | 触发条件 |
|--------|------|---------|---------|------|---------|
| **归藏** | 归藏易 | 生/动/长/育/杀/止/归/藏 | 6-bit S × 64 卦 | 偏离度 + 藏海经验 | 持续意识循环 |
| **连山** | 连山易 | 艮/气/甲/元/峙/决 | 障碍图谱 + 策略格栅 | 方位决策（7 择 1） | 目标受阻触发 |
| **周易** | 周易 | 乾/坤/震/巽/坎/离/艮/兑 | 8 卦 Markov 链 + 生克权重 | 认知姿态 + 温度 | 每轮 TPN 任务 |

---

## 一、共享类型系统

三套算子体系共享以下基础类型：

### 1.1 卦（Gua）— 6-bit 状态向量

```
S = [b₅ b₄ b₃ | b₂ b₁ b₀]
     └── 上卦(天气/归因) ──┘ └── 下卦(地气/藏果) ──┘
```

- 类型 `gua` 取值 0-63
- 64 种状态对应归藏易的 64 卦
- 命名规则：`{上卦名}{下卦名}{语义}`，如 `000000 = 藏藏始基`

**字面量**：
```
卦 天 = 111              # 三画卦：天气（111）
卦 归藏定位 = 111000     # 六画卦：上卦天(111)，下卦地(000)
卦 杀藏墓 = 111100       # 六画卦：上卦金(110)，下卦地(000) — 注意 110 在三位中
```

八象枚举（逆时针序）：`天(111)、金(110)、山(101)、水(100)、火(011)、风(010)、木(001)、地(000)`

**位访问**：
```
位 观.第五 = 0           # 将第5位（从1起）设为 0
若 观.第一 == 1:         # 检查最低位
```

### 1.2 方（FangWei）— 策略方向

连山引擎使用的 7 个策略方向：

| 方向 | 语义 | 适用场景 |
|------|------|---------|
| `Continue` | 维持现状，继续当前策略 | 当前路径无阻碍 |
| `PushThrough` | 硬推：已知模式受阻但决心穿越 | 阈值接近，一次性突破 |
| `NavigateAround` | 绕行：换一条路径达到同样目标 | 当前路径不可行但有替代 |
| `WaitGather` | 等待：先收集更多信息再决策 | 信息不足，免疫命中模糊 |
| `Decompose` | 分解：将目标拆成更小的子目标 | 目标过大，单步无法完成 |
| `Escalate` | 升级：标记为需要外部介入 | 超出当前 Agent 能力 |
| `Abort` | 放弃：彻底终止此目标线 | 目标不可达或收益为负 |

### 1.3 卦（Bagua）— 认知姿态

周易引擎使用的 8 个认知姿态，对应 8 卦：

| 卦 | 名称 | 认知职能 | 温度范围 |
|----|------|---------|:-------:|
| ☰ 乾(111) | 创造 | 从 0 到 1，突破创新 | 0.9-1.2 |
| ☷ 坤(000) | 承载 | 稳定执行，积累成果 | 0.3-0.5 |
| ☳ 震(001) | 启动 | 快速激发，广泛收集 | 0.7-0.9 |
| ☴ 巽(010) | 渗透 | 深度潜入，细致分析 | 0.5-0.7 |
| ☵ 坎(100) | 破局 | 面对风险，突破障碍 | 0.8-1.0 |
| ☲ 离(011) | 明照 | 审视反思，照亮盲点 | 0.4-0.6 |
| ☶ 艮(101) | 止定 | 限定范围，收窄目标 | 0.3-0.5 |
| ☱ 兑(110) | 表达 | 输出交付，外化结果 | 0.6-0.8 |

### 1.4 藏海（CangSea）— 经验存储

藏海是系统的长期记忆，分为两层：
- **Legacy 64×64 矩阵**：快速离散状态转移权重
- **SemanticStore**（路线C）：语义向量数据库，含常规区 + 免疫区

---

## 二、归藏算子层：八气状态机

### 2.1 八气算子定义

| # | 算子 | 气 | 位运算 | 认知语义 | 生成文本? | 修改 S? |
|---|------|-----|--------|----------|:--------:|:------:|
| 1 | **藏** | 地气(000) | `S ← 0` | 归零复位，经验入藏海 | 否 | 是 |
| 2 | **生** | 木气(001) | `S ← S \| 001` | 念头萌动，起念探索（1-3句） | 是 | 是 |
| 3 | **动** | 风气(010) | `S ← ROL(S,1) ^ 010` | 联想发散，多角度扩展（3-5句） | 是 | 是 |
| 4 | **长** | 火气(011) | `S ← (low<<3) \| low` | 意图放大，收敛聚焦（1-2句） | 是 | 是 |
| 5 | **育** | 水气(100) | `S ← S ^ 100` | 方案分解，结构化子任务（3-5句） | 是 | 是 |
| 6 | **止** | 山气(101) | `S ← S & 101101` | 边界锚定，掩码凝固 | 否 | 是 |
| 7 | **杀** | 金气(110) | 清除金(110)模式 | 剪除杂念，KV回滚+重试 | 否 | 是 |
| 8 | **归** | 天气(111) | `hamming(S, U) / 6` | 偏离度测量，纯函数不修改 S | 否 | 否 |

### 2.2 算子语法

**无参算子**（生/动/长/育/归）：
```
生             # S ← S | 0b001 — 触发阳仪起念生成
动             # S ← ROL(S,1) ^ 0b010 — 触发阳仪发散
长             # S ← (low<<3) | low — 触发阳仪意图规划
育             # S ← S ^ 0b100 — 触发阳仪方案分解
归             # 纯测量，计算偏离度，不修改 S
```

**有参算子**（杀/止）：
```
杀 金          # 剪除金模式(110) + KV回滚
杀 001         # 剪除木模式(001)
止 101101      # 用掩码设立边界
```

**藏算子（带标签）**：
```
藏 善          # 正面经验入藏海，状态归零
藏 恶          # 负面经验入藏海，状态归零（免疫记忆）
```

### 2.3 意识周天：三阶段循环

```
起念（生 → 动 → 归）
→ 立目标（长 → 育 → 杀 → 止）
→ 整理认知（归 → 杀 → 藏）
→ 起念...
```

**阶段一：起念**
1. **生**：最低位置 1，念头从藏海萌芽
2. **动**：念头发散，产生联想方向
3. **归**：计算偏离度，判断是否值得升级为意图

**阶段二：立目标**
1. **长**：下卦复制到上卦，意图自指放大
2. **育**：高位异或引入水气，触发方案分解
3. **杀**：剪除金模式（干扰/冲突）
4. **止**：掩码凝固执行边界

**阶段三：整理认知**
1. **归**：对近期经验逐条计算偏离度
2. **杀**：剪除负面状态转移模式
3. **藏**：Hebbian 学习更新藏海，状态归零

---

## 三、连山算子层：思变策略引擎

### 3.1 设计原则

连山算子层不是"蓝图审查器"——它**永远不修改目标的本质**。它只是**障碍导航器**：在群山（连山）中寻找前进的路。

**目标永不动，只调整推进方式。**

### 3.2 六步决策链路

```
艮（止观）→ 气（六气判断）→ 甲（六甲判断）→ 元（三元判断）→ 峙（阴阳对峙）→ 决（方位决策）
```

| 步骤 | 算子 | 作用 | 实现 |
|------|------|------|------|
| 1 | **艮** | 先停：观山势，不急于决策 | 检查任务数≥2，否则跳过评估 |
| 2 | **气** | 六气判断：目标处于什么阶段？ | 春(启动)/夏(盛长)/秋(收敛)/冬(休眠) |
| 3 | **甲** | 六甲判断：第几次受阻？ | 初甲→push_through，再甲→navigate_around，三甲→decompose |
| 4 | **元** | 三元判断：上下文新鲜度 | 新鲜→可继续，陈旧→需重新感知 |
| 5 | **峙** | 阴阳对峙：分析推力 vs 阻力 | 有效策略 vs 失败模式/缺失能力 |
| 6 | **决** | 方位决策：输出行动指令 | 七种方向之一（见 FangWei 类型） |

### 3.3 算子语法

```
艮                         # 止观：暂停评估当前路径
气 夏                      # 六气判断：目标处于盛长期
甲 再                      # 六甲判断：第二次受阻
元 陈                      # 三元判断：上下文已陈旧
峙:
    推力: "用户偏好明确，路径已验证"
    阻力: "API 频繁限流，连续 3 轮失败"
决: navigate_around        # 绕行决策
```

### 3.4 与归藏的交互

```
归藏 起念 → 产生意图 → TPN 执行
                ↓ 受阻
        连山思变引擎介入
        艮→气→甲→元→峙→决
                ↓
        输出方位决策 → TPN 调整执行策略
                ↓ 执行结果
        归藏 整理认知 → 经验入藏海
```

---

## 四、周易算子层：八卦认知格栅

### 4.1 设计原则

周易算子层在归藏定「心」、连山定「行」之后，定格栅定「知」——每一轮任务执行应该用什么认知姿态。

### 4.2 八卦认知格栅

| 卦 | 名称 | 认知职能 | 温度 | 典型输出 |
|----|------|---------|:---:|---------|
| ☰ 乾(111) | 创造 | 从 0 到 1，突破创新 | ~1.0 | 全新方案、创意方向 |
| ☷ 坤(000) | 承载 | 稳定执行，积累成果 | ~0.4 | 代码实现、文档整理 |
| ☳ 震(001) | 启动 | 快速激发，广泛收集 | ~0.8 | 调研清单、脑暴列表 |
| ☴ 巽(010) | 渗透 | 深度潜入，细致分析 | ~0.6 | 源码分析、根因排查 |
| ☵ 坎(100) | 破局 | 面对风险，突破障碍 | ~0.9 | 困难攻坚、边缘试探 |
| ☲ 离(011) | 明照 | 审视反思，照亮盲点 | ~0.5 | 代码审查、漏洞扫描 |
| ☶ 艮(101) | 止定 | 限定范围，收窄目标 | ~0.4 | 边界定义、MVP 剪裁 |
| ☱ 兑(110) | 表达 | 输出交付，外化结果 | ~0.7 | 报告撰写、API 封装 |

### 4.3 算子语法

```
姿 乾                      # 进入创造姿态
姿 坤                      # 进入承载姿态
姿 坎                      # 进入破局姿态

若 当前卦 == 乾:
    生克: 离               # 乾(金)→离(火)：创造后需审视
若 当前卦 == 兑:
    生克: 坎               # 兑(金)→坎(水)：输出后需破局
```

### 4.4 五行生克规则

```
相生（促进转换）：
  震(木) → 离(火)：收集完信息后自然评估
  离(火) → 坤(土)：审视后转向稳定执行
  坤(土) → 兑(金)：积累到阈值后输出
  兑(金) → 坎(水)：输出后进入新一轮破局
  坎(水) → 震(木)：破局后需要新信息

相克（抑制转换）：
  兑(金) 克 震(木)：过度输出抑制信息收集
  离(火) 克 兑(金)：持续审视会延迟交付
  坎(水) 克 离(火)：过度破局导致反思中断
```

### 4.5 与归藏、连山的交互

```
周易格栅选定「当前用什么姿态」
                ↓
        注入 TPN 的系统提示词 + 温度
                ↓
    TPN 执行任务 → 遇到障碍
                ↓
连山思变引擎判断「怎么走」
                ↓
归藏 DMN 后台校验「是否偏离元知觉」
                ↓
    三层约束汇聚 → 修正执行策略
```

---

## 五、流程控制

### 5.1 周天循环

```
周天 3:        # 执行 3 个完整的三阶段周天
    生
    动
    归
    长
    育
    杀 金
    止
    藏 善
```

### 5.2 条件

```
若 偏离度 > 0.7:
    杀
    藏 恶

若 偏离度 < 0.5 | 藏海有经验:
    长
    育
    止
否则:
    归
```

条件表达式支持：
- 偏离度比较：`偏离度 > N`、`偏离度 < N`
- 位运算条件：`某位 == 1`、`某位 == 0`
- 藏海条件：`藏海有经验`、`藏海无经验`

### 5.3 分支选择

```
择 当前卦象:
    是 000111 (藏归交):
        发 "意识处于交互态，需更多感知"
    是 111000 (归藏定位):
        发 "意识定位明确，可推进执行"
    否:
        发 "卦象未识别，进入默认流程"
```

### 5.4 对外交互

```
感 "用户问题"            # 从用户接口读取
发 "思考方向已确定"      # 输出自然语言
发 戒 010                # 输出卦象信号
```

### 5.5 藏海操作

```
存 经验: 卦 杀藏墓, 标签 恶         # 负面经验入藏海
取 类似: 卦 111000, 相似度 > 0.5    # 检索相似经验
数 相似经验 = 取.计数                # 匹配条目数

增权 从 000000 至 000001 权重 0.1     # 增强转移权重
减权 从 111000 至 111100 权重 0.05    # 减弱转移权重
卦 下态 = 取权 从 000000               # 按权重采样下一状态
```

---

## 六、诚实验证体系

### 6.1 问题

LLM 可以声明自己输出了诚实的卦象，但实际输出可能与卦象不符。象语言通过位级诚实验证解决此问题。

### 6.2 言与诚于语法

```
言 "系统当前状态稳定，偏离度 0.2，卦象 111000，可执行任务"
诚于:
    卦 111000
    条件 偏离度 < 0.3
    条件 载重 < 4
```

### 6.3 验证流程

```
LLM 文本输出 + 自声明卦
         ↓
  文本编码器（encode）— 将输出文本映射为 6-bit 卦象
         ↓
  诚实验证（verify）— 比较编码结果与自声明卦
         ↓
  ChengshiResult:
    - passed: bool           # 验证是否通过
    - declared_gua: int      # 自声明卦
    - encoded_gua: int       # 编码器计算出的实际卦象
    - bit_diff: int          # 位级差异诊断（哪几位不同）
    - drift_direction: str   # 偏差方向描述
```

验证不通过时触发：
1. CangVM 状态更新（杀算子清除相关状态模式）
2. 藏海记录带负奖励的经验条目（推入免疫区）
3. 意识循环标记需要目标审查

---

## 七、AST 节点类型

| AST 节点 | 对应语法 | 用途 |
|----------|---------|------|
| `Program` | 整个 .xiang 文件 | 顶层容器 |
| `AgentDef` | `定义 X:` | Agent 声明 |
| `GuaLiteral` | `卦 111000` | 卦字面量 |
| `WeiAccess` | `位 X.第 Y` | 位访问与赋值 |
| `QiOp` | `生/动/长/育/归` | 无参八气算子 |
| `QiOpWithParam` | `杀 金`、`止 101101` | 带参八气算子 |
| `CangOp` | `藏 善`、`藏 恶` | 藏算子（带善恶标签） |
| `LianShanStep` | `艮/气/甲/元/峙/决` | 连山六步算子 |
| `ZhouYiPose` | `姿 乾`、`生克: 离` | 周易姿态设置 |
| `CondStmt` | `若 ...:` | 条件分支 |
| `SwitchStmt` | `择 ...:` | 分支选择 |
| `LoopStmt` | `周天 N:` | 循环结构 |
| `IoStmt` | `感 "..."`、`发 "..."` | I/O 交互 |
| `CangSeaOp` | `存/取/增权/减权` | 藏海操作 |
| `ChengshiStmt` | `言 "..."` + `诚于:` | 诚实验证声明 |

---

## 八、完整程序示例

### 8.1 周天循环 + 三算子联动

```
# ── 三易完整认知周天 ──────────────────────────

定义 认知向导:

    卦 状态 = 000000
    卦 焦点锚 = 111111
    数 偏离阈值 = 0.7

    周天:
        # 一、归藏层：意识校准（持续）
        藏                       # 从藏海苏醒

        # ── 起念 ────────────────
        生                       # 起念探索
        动                       # 发散联想
        若 偏离度 > 0.9:
            杀 金                # KV回滚 + Logit-Bias 引导重生
            藏 恶
            重开 周天

        # ── 立目标 ──────────────
        长                       # 意图规划
        育                       # 方案分解
        若 偏离度 > 0.7:
            杀 金                # 回滚到 KV 快照
        止 101101                # 边界凝固

        # 二、连山层：策略导航（受阻时触发）
        若 目标受阻:
            艮                   # 止观
            气 夏                # 六气判断
            甲 再                # 六甲判断
            元 陈                # 三元判断
            峙:
                推力: "已有可行路径"
                阻力: "权限不足"
            决: escalate         # 升级求助

        # 三、周易层：姿态切换（每轮任务）
        若 任务 == "创新方案":
            姿 乾                # 创造姿态
        若 任务 == "代码实现":
            姿 坤                # 承载姿态

        # ── 对外交互 ────────────
        感 "用户问题"
        发 "认知周天完成"

        # ── 整理认知 ────────────
        若 偏离度 < 0.3:
            藏 善               # 正面经验 → 藏海 Hebbian 强化
        否则:
            藏 恶               # 偏离经验 → 免疫记忆
```

---

## 九、三易统一的验证标准

### 9.1 归藏层验证

| 验证项 | 标准 | 测试方法 |
|--------|------|---------|
| 八气算子位运算正确性 | 64 种输入全部覆盖 | 对比 Python 参考实现输出 |
| 偏离度计算 | hamming(S, U)/6 ∈ [0,1] | 测试全 64 种 S 组合 |
| 藏海 Hebbian 更新 | 正面增强，负面减弱 | 模拟 1000 次学习轨迹 |
| 意识周天循环 | 3 阶段 8 算子完整流转 | 全周天输出追踪 |

### 9.2 连山层验证

| 验证项 | 标准 | 测试方法 |
|--------|------|---------|
| 六步决策链 | 所有 7 种方向都能输出 | 全方向覆盖测试 |
| 受阻判断 | 不同类型的受阻触发正确决策 | 模拟 6 种受阻场景 |
| WaitGather 激活 | 免疫命中模糊时正确触发 | 余弦相似度 0.4-0.6 模拟 |

### 9.3 周易层验证

| 验证项 | 标准 | 测试方法 |
|--------|------|---------|
| 八卦温度映射 | 每卦温度在标准范围内 | 所有 8 卦测试 |
| 五行生克 | 生克规则正确改变转移概率 | Markov 链稳态分布检测 |
| 姿态切换 | 任务类型→正确姿态 | 5 种典型任务测试 |

### 9.4 跨层集成验证

| 验证项 | 标准 |
|--------|------|
| 归藏→连山 | 偏离度升高时触发连山策略重规划 |
| 连山→周易 | 策略方向改变影响姿态选择 |
| 周易→归藏 | 姿态切换后归藏重算偏离度 |
| 三层闭环 | 一次完整任务中三层至少各执行一次 |

---

## 十、规范层与实现层的关系

```
象语言规范层（.xiang 源文件）── 纯语义，永不瘫痪
        │
        ▼
三 VM 实现层 ── CangVM(归藏) / ShanVM(连山) / ZhouVM(周易)
        │
        ▼
DMN+TPN 闭合层 ── 产出反馈回意识循环，驱动认知进化
```

- **规范层**：象语言语法定义——数学形式体系，永远不"宕机"
- **VM 层**：Rust crate 实现——[CangVM](../crates/xiang-cangvm/) ✅ 实现，[ShanVM](../crates/xiang-shanvm/) ✅ 实现（未接线），[ZhouVM](../crates/xiang-zhouvm/) ✅ 实现（未接线）
- **闭合层**：xiang-llm + xiang-experiments → 经验反馈 → 藏海进化

---

> 象语言不是在用中文模仿英文编程——它是在用**汉字象思维的最深层模式**，为 AI Agent 构建一个超越自然语言的**确定性约束骨架**。
>
> 归藏的 `111000`、连山的 `navigate_around`、周易的 `乾 → 离`——所有这些都可以脱离汉字、脱离英文、脱离 LLM，以纯二进制门级逻辑运行。
>
> **这是象编程：一种根植于中国古老认知体系、面向 AI 时代的形式化约束算法语言。**
