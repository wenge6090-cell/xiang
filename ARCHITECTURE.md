# 象 (Xiang) — 三易智能约束系统 架构文档

> **基于象语言的三套易经算子体系（归藏/连山/周易）统一推理时认知约束框架。**


---

## 目录

1. [项目定位](#一项目定位)
2. [名称说明](#二名称说明)
3. [系统架构](#三系统架构)
4. [三引擎分工](#四三引擎分工)
5. [象语言：统一约束语言](#五象语言统一约束语言)
6. [路线A：阶段约束（已实现）](#六路线a阶段约束已实现)
7. [路线B：控制原语微调（设计完成，待实施）](#七路线b控制原语微调设计完成待实施)
8. [路线C：太极双LLM（远期规划）](#八路线c太极双llm远期规划)
9. [硬件约束与当前瓶颈](#九硬件约束与当前瓶颈)
10. [技术栈](#十技术栈)
11. [Crate 依赖关系](#十一crate-依赖关系)
12. [贡献指南](#十二贡献指南)

---

## 一、项目定位

**象 (Xiang)** 是一套 **模型无关的推理时认知约束框架**，基于象语言 (XiangLang) 统一三套独立的易经算子体系（归藏/连山/周易），在推理时通过三层程序化干预引导 LLM 输出。

这不是推理增强工具，不是上下文管理器，不是提示词工程。这是一个**具备初步元认知能力的、可自我进化的小模型 Agent 框架**。

### 核心理念

- **零提示词注入**：所有约束逻辑由 Rust 程序化执行，不依赖自然语言 prompt
- **模型无关**：约束引擎独立于具体 LLM，支持任何可通过 API/FFI 调用的模型
- **三层干预**：生成前（Gua 状态计算 + 温度策略）→ 生成中（Logit-bias 概率调整）→ 生成后（阴仪协议校验 + 违规回滚）
- **可自举进化**：约束引擎产生的结构化数据可反哺模型微调（路线B），形成数据飞轮

---

## 二、名称说明

| 名称 | 说明 |
|------|------|
| **象 (Xiang)** | 项目总称，取自"象语言"——三套算子的统一约束语言 |
| **归藏 (GuiZang)** | 第一引擎 CangVM —— 执行控制器，周天算子循环 |
| **连山 (LianShan)** | 第二引擎 ShanVM —— 策略导航，障碍检测与方位决策 |
| **周易 (ZhouYi)** | 第三引擎 ZhouVM —— 认知姿态，八卦网格与温度调度 |
| **三易** | 三套引擎的统称 |
| **象语言 (XiangLang)** | 统一三套算子体系的 DSL（领域特定语言） |

---

## 三、系统架构

```
                    ┌──────────────────────────────┐
                    │   ShanVM (连山)               │
                    │   策略引擎 · 障碍导航          │
                    │   6步决策链: 艮→气→甲→元→峙→决 │
                    │   输出 7 种方位决策            │
                    └──────────┬───────────────────┘
                               │ 策略建议
                               ▼
┌─────────────────────────────────────────────────┐
│              CangVM (归藏)                        │
│          执行控制器 · 8算子周天                   │
│                                                   │
│  生→动→归→长→育→杀→止→藏                         │
│  决策: PASS / ROLLBACK / SKIP / STOP              │
│                                                   │
│  生成前: Gua状态机 → 偏离度 → 温度策略            │
│  生成中: XiangLogitBias → token概率调整           │
│  生成后: YinProtocolChecker → 格式验证 → 违规回滚 │
└──┬──────────────┬──────────────────┬─────────────┘
   │              │                  │
   ▼              ▼                  ▼
┌──────────┐  ┌──────────┐    ┌──────────┐
│ 阴仪协议  │  │ ZhouVM   │    │ CangSea  │
│ 正则校验  │  │ (周易)    │    │ 经验矩阵  │
│ 格式验证  │  │ 8卦网格   │    │ Hebbian  │
│ 偏离检测  │  │ 五行生克  │    │ 权重衰减  │
│          │  │ 温度调度  │    │ 语义存储  │
└──────────┘  └──────────┘    └──────────┘
```

### 执行流水线（单轮）

```
用户输入
   │
   ▼
┌─────────────────────────────┐
│ 1. CangVM 状态计算           │  ← 6-bit Gua → Hamming 偏离度
│    · 温度策略                │     偏离<0.3 → 0.8, >0.7 → 0.3
│    · 阶段算子选择（生/动/长/育）│
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ 2. LLM 生成（带 Logit-bias） │  ← XiangLogitBias
│    · 离焦token 压制 (-0.5~-8)│     连续强度缩放
│    · 发散token 轻度压制       │
│    · EOS 提升 (+0~+4)       │
│    · 偏离>0.95 强制截断      │
└─────────────┬───────────────┘
              │ 生成文本
              ▼
┌─────────────────────────────┐
│ 3. 阴仪协议校验              │  ← YinProtocolChecker
│    · 正则匹配算子规则         │     生/动/长/育 各4-8条正则
│    · 违规 → SHA+ROLLBACK    │
│    · 合规 → 追加到历史       │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ 4. CangVM 经验沉淀           │  ← CangSea Hebbian 矩阵
│    · 成功/失败 → 权重更新    │
└─────────────────────────────┘
```

---

## 四、三引擎分工

| 引擎 | 名称 | 渊源 | 职责 | 实现状态 |
|:----:|:----:|:----:|------|:--------:|
| **CangVM** | 归藏 (GuiZang) | 《归藏易》 | 执行控制器：算子周天、偏离度计算、Logit-bias 生成、阴仪校验 | ✅ 完整实现 |
| **ShanVM** | 连山 (LianShan) | 《连山易》 | 策略导航：6步障碍检测链（艮→气→甲→元→峙→决），7种方位决策 | ✅ 代码就绪，未接入流水线 |
| **ZhouVM** | 周易 (ZhouYi) | 《周易》 | 认知姿态：8卦网格、五行生克权重、温度调度 | ✅ 代码就绪，未接入流水线 |

### 三引擎协同闭环

```
用户请求 → 周易选姿态 → 归藏执行周天 → 遇障碍 → 连山导航 → 归藏执行策略
                                                              ↓
                                                        藏海沉淀经验
                                                              ↓
                                                        经验回流各引擎
```

---

## 五、象语言：统一约束语言

**象语言不是归藏的语法糖。它是三套独立算子体系的统一约束语言。**

三套引擎共享同一套底层类型系统（定义在 `xiang-core` 中）：

| 共享类型 | 作用 | 被哪些引擎使用 |
|---------|------|--------------|
| **Gua(u8)** | 6 位卦象状态（0-63） | 全部三引擎 |
| **八气算子** | 生/动/归/长/育/杀/止/藏 | CangVM 执行，ShanVM 输出策略，ZhouVM 调整姿态 |
| **FangWei** | 7 种方位决策 | ShanVM 产出，CangVM 消费 |
| **Bagua** | 8 卦 + 生克权重矩阵 | ZhouVM 核心，CangVM 读取温度 |
| **CangSea** | 64×64 Hebbian 经验矩阵 + 语义存储 | 归藏沉淀，连山/周易消费 |

这种共享类型体系意味着：
1. **单条训练数据包含三层标签**：归藏的算子序列 + 偏离度、连山的方位决策 + 障碍等级、周易的卦象姿态 + 生克权重
2. **云端评判可以精确到每一层**：不仅评判"偏离度是否合理"，还评判"这个障碍下选择绕行是否合理"、"当前用震卦姿态是否合适"
3. **三层引擎各自积累经验**：归藏的藏海、连山的策略格栅、周易的转移权重，都可以从成功/失败频率中自调整

象语言语法示例：

```
定义 思维向导:
    卦 焦点状态 = 111111
    生
    动
    归
    若 偏离度 > 0.7:
        杀 110
        藏 恶
    若 偏离度 < 0.3:
        藏 善
    否则:
        止 101101
        藏 善
```

---

## 六、路线A：阶段约束（已实现）

路线A 通过 **外部程序化干预** 对 LLM 生成施加约束：

1. **生成前**：Gua 状态机计算当前算子 + 偏离度 → 温度策略
2. **生成中**：XiangLogitBias 调整 token 概率（连续强度缩放 0.5~-8.0）
3. **生成后**：YinProtocolChecker 正则校验 → 合规则追加到历史，违规则 KV 回滚

| 指标 | 对照组 (裸跑) | 实验组 (三易控制) | 改善 |
|:----|:----:|:----:|:----:|
| 偏离率 | 0.6351 | 0.2809 | **-55.8%** |
| 结构合规率 | 0.4040 | 0.8712 | **+115.7%** |
| 完成率 | 0.7850 | 1.0000 | **+27.4%** |

> 详细规格见 [specs/01-路线A-阶段约束.md](./specs/01-路线A-阶段约束.md)

---

## 七、路线B：控制原语微调（可选方案，硬件升级后执行）

路线B 将八气算子通过 **词表扩展 + 控制原语嵌入 + LoRA 微调** 内化为模型自身的控制原语，实现零自然语言指令的硬控制。

**当前硬件不可行**：RX 6650 XT (8GB) + Windows 环境下无可用 QLoRA 训练工具链。连山/周易流水线接入已从路线B 解耦为独立任务。

> 详细规格见 [specs/02-路线B-控制原语微调.md](./specs/02-路线B-控制原语微调.md)

---

## 八、路线C：太极双LLM（远期规划）

路线C 引入**阴仪 TextEncoder**，使三引擎从"离散查表"跃升为"全语义驱动"：
- **归藏**：hybrid_deviation = α·hamming + (1-α)·cosine
- **连山**：V_goal + V_obstacle → V_strategy（语义策略分类器）
- **周易**：V_origin + V_strategy → V_attitude（认知姿态编码器）
- **藏海**：从 64×64 矩阵升级为语义向量数据库（SemanticStore）

前置条件：连山/周易流水线接入完成 + 阴仪编码基础设施（不依赖路线B 微调）。

> 详细规格见 [specs/03-路线C-太极双LLM.md](./specs/03-路线C-太极双LLM.md)  
> 工程实施见 [specs/路线C-实施计划.md](./specs/路线C-实施计划.md)

---

## 九、硬件约束与当前瓶颈

### 开发环境

| 组件 | 规格 |
|------|------|
| CPU | Intel i5-10600KF |
| GPU | AMD RX 6650 XT (8GB VRAM, RDNA2/gfx1032) |
| 内存 | 48GB DDR4 |
| 操作系统 | Windows 10 / WSL 混合 |
| 模型 | Qwen3.5-4B-Uncensored (Q4_K_M, ~2.5GB) |
| 推理框架 | llama.cpp (Vulkan, 64K 上下文) |

### 推理性能

| 操作 | 性能 | 说明 |
|------|:----:|------|
| 4B Q4_K_M 推理 (GPU) | ~2.3 tok/s | RX 6650 XT 单 batch |
| Vocab 扫描 (147+26 tokens) | ~3-5s | 一次性，缓存 |
| 首次冷生成 (80 tokens) | ~30-35s | cache_prompt 后降至 ~20-25s |

### 训练瓶颈（路线B）

| 项目 | 状态 | 说明 |
|:----:|:----:|------|
| 4B QLoRA (本地 GPU) | ❌ 不可行 | AMD Windows 无可用训练工具链 |
| 4B QLoRA (外部训练环境) | ✅ 可行 | 需另行准备训练硬件 ~3-4h |
| 0.5B LoRA (本地) | ⚠️ 可能 | 模型太小，训练数据质量不足 |
| 训练数据导出 (CangSea) | 📋 待实现 | 藏海 → JSONL pipeline 未完成 |

---

## 十、技术栈

### 后端（Rust）

| 技术 | 用途 |
|------|------|
| Rust 2024 edition | 全部 9 个 crate |
| axum + tokio | HTTP 服务 (xiang-chat) |
| serde / serde_json | 序列化 |
| ureq | HTTP 客户端 (HttpBackend) |
| llama.cpp (Vulkan) | GGUF 模型推理 |
| rand | 随机数生成 (ZhouVM Markov) |
| uuid | 唯一标识 (CangSea) |

### 前端

| 技术 | 用途 |
|------|------|
| React 18 + TypeScript | UI |
| Vite 6 | 构建工具 |
| Tailwind CSS 3.4 | 样式 |

### 约束引擎核心

| Crate | 行数(约) | 测试数 | 职责 |
|-------|:--------:|:------:|------|
| xiang-core | ~2000 | 77 | Gua/Bagua/偏离度/CangSea/阴仪协议 |
| xiang-parser | ~1000 | 12 | 象语言 词法/语法/AST |
| xiang-cangvm | ~1500 | 60 | CangVM 执行引擎 + 三引擎编排 |
| xiang-shanvm | ~400 | 17 | 连山 6步策略链 |
| xiang-zhouvm | ~300 | 14 | 周易 8卦网格 |
| xiang-verifier | ~500 | 14 | 可信验证 |
| xiang-llm | ~700 | 16 | Logit-bias/LLM 后端抽象 |
| xiang-experiments | ~2000 | 33 | 实验框架/基准测试 |
| xiang-chat | ~1500 | 2 | Axum 聊天服务 |

---

## 十一、Crate 依赖关系

```
xiang-chat
  ├── xiang-cangvm
  │    ├── xiang-core
  │    ├── xiang-parser
  │    ├── xiang-shanvm
  │    └── xiang-zhouvm
  ├── xiang-llm
  ├── xiang-shanvm
  └── xiang-core

xiang-experiments (CLI)
  ├── xiang-cangvm
  ├── xiang-llm
  └── xiang-core

xiang-verifier
  ├── xiang-core
  └── xiang-parser
```

---

## 十二、三层干预架构

> 从第一性原理出发，三易协同工作分为三个层次：观测（Observe）→ 引导（Guide）→ 管理（Manage）。
> **观测优先，引导其次，清理最后。不压制，只引导。不预防，只清理。**

### 12.1 三引擎 × 三层次

```
注意力相位层 ─ 周易 (ZhouVM)
  对应 LLM 内部：标准 Self-Attention（全连接注意力）
  职责：设定注意力的"工作模式"
  机制：通过温度调度改变注意力的行为风格
        乾(创造/1.2) → 注意力敢于远距离跳转
        离(分析/0.5) → 注意力精确收敛
        艮(审慎/0.3) → 注意力最集中，不漂移

注意力阶段层 ─ 归藏 (CangVM) — 生成树
  对应 LLM 外部：Logit-Bias 方向引导
  职责：标记认知阶段，实时干预 token 概率方向
  机制：算子差异化 Logit-Bias（±1.0 上限）
        生 → 鼓励探索性 token
        动 → 鼓励发散性 token
        长 → 鼓励收敛性 token
        育 → 鼓励结构性 token

注意力策略层 ─ 连山 (ShanVM)
  对应 LLM 类比：稀疏注意力（选择性关注）
  职责：遇到障碍时切换注意力策略路径
  机制：六步决策链 → 7 种方位决定注意力方向
```

### 12.2 生成树与压缩树的分工

```
生成树（生→动→长→育）—— 生成时干预：
  通过 Logit-Bias 在每步 token 生成前调整概率分布
  不压制"错误 token"，而是改变 token 空间的重心方向
  强度上限 ±1.0（远低于早期版本 -8.0）

压缩树（杀→止→藏）—— 生成后管理：
  只做上下文管理，不做语义判断
  杀：删除已偏离的对话历史
  止：冻结上下文，不再追加新信息
  藏：清空上下文，重置认知起点
```

### 12.3 幻觉与涌现

LLM 的幻觉与涌现同源，都来自连续向量空间的统计生成。三易不试图消灭幻觉——三易在给涌现提供轨道。过度压制 token 概率（如早期 Logit-Bias 的 -8.0）会同时杀死涌现。

三易当前在"内容真实性"维度上不提供保证——它无法验证模型输出的事实是否正确。这是路线C（太极双LLM，独立阴 LLM 做事实校验）的职责范围。

### 12.4 闭环数据流

```
1. 归：观测 → EmbeddingObserver 计算语义偏离度
2. 周易：姿态 → 选择温度/行为风格
3. 归藏：算子阶段 → 决定当前 Logit-Bias 方向
4. 连山（可选）：策略 → 偏离度高时切换策略方向
5. LLM 生成 → 带算子树导 Logit-Bias
6. 归：再次观测 → 记录汉字投影轨迹
7. 阴仪校验 → 格式合规检查
8. 归藏：judge() → Pass/Rollback/Skip/Stop/Finish
9. 执行上下文操作（杀/止/藏）
10. 周天循环或结束
```

---

## 十三、贡献指南

### 最需要的贡献

| 领域 | 说明 | 难度 |
|------|------|:----:|
| **路线B 训练 pipeline** | 构建 QLoRA 微调脚本（外部训练环境优先） | ⭐⭐⭐ |
| **AMD GPU 训练支持** | 解决 Windows AMD 平台训练工具链兼容性 | ⭐⭐⭐⭐ |
| **ShanVM 流水线接入** | 将连山策略引擎接入 CangVM 主循环 | ⭐⭐ |
| **ZhouVM 流水线接入** | 将周易姿态引擎接入 CangVM 主循环 | ⭐⭐ |
| **CangSea 数据导出** | 实现藏海训练数据导出为 JSONL | ⭐⭐ |
| **新算子规则** | 为 YinProtocolChecker 添加更多正则规则 | ⭐ |
| **前端增强** | chat-ui 交互优化、可视化 | ⭐ |

### 开发环境要求

- Rust 2024 edition (nightly)
- Node.js 18+（前端）
- llama.cpp (Vulkan)：`-DGGML_VULKAN=ON`
- 模型文件：任意 GGUF 格式（推荐 Qwen 系列）
- 最低硬件：4GB VRAM + 16GB 内存

### 本地快速开始

```bash
# 构建
cd xing && cargo build

# 启动全部服务
./start_all.bat

# 运行 Mock 基准测试
cargo run --bin xiang-exp -- bench --mock offfocus --seed 42

# 运行真实模型基准测试
cargo run --bin xiang-exp --features http_backend -- bench --http http://localhost:8080

# 运行全部测试
cargo test
```

---

> **象 (Xiang)** — 三易智能约束系统，基于象语言。归藏、连山、周易，三套易经算子体系统一于同一框架。
>
> *归藏是执行者，连山是导航者，周易是姿态者。三者合一，构成完整的认知约束闭环。*
>
> 技术规格见 [specs/](./specs/) 目录。
