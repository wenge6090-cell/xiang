# 象 (Xiang) — 三易智能约束系统 架构文档

> **基于象语言的三套易经算子体系（归藏/连山/周易）统一推理时认知约束框架。**


---

## 目录

1. [项目定位](#一项目定位)
2. [名称说明](#二名称说明)
3. [系统架构](#三系统架构)
4. [三引擎分工](#四三引擎分工)
5. [象语言：统一约束语言](#五象语言统一约束语言)
6. [路线A：阶段约束（已实现）](#六路线a阶段约束已实现)
7. [路线B：控制原语微调（设计完成，待实施）](#七路线b控制原语微调设计完成待实施)
8. [路线C：太极双LLM（远期规划）](#八路线c太极双llm远期规划)
9. [硬件约束与当前瓶颈](#九硬件约束与当前瓶颈)
10. [技术栈](#十技术栈)
11. [Crate 依赖关系](#十一crate-依赖关系)
12. [贡献指南](#十二贡献指南)

---

## 一、项目定位

**象 (Xiang)** 是一套 **模型无关的推理时认知约束框架**，基于象语言 (XiangLang) 统一三套独立的易经算子体系（归藏/连山/周易），在推理时通过三层程序化干预引导 LLM 输出。

这不是推理增强工具，不是上下文管理器，不是提示词工程。这是一个**具备初步元认知能力的、可自我进化的小模型 Agent 框架**。

### 核心理念

- **零提示词注入**：所有约束逻辑由 Rust 程序化执行，不依赖自然语言 prompt
- **模型无关**：约束引擎独立于具体 LLM，支持任何可通过 API/FFI 调用的模型
- **三层干预**：生成前（Gua 状态计算 + 温度策略）→ 生成中（Logit-bias 概率调整）→ 生成后（阴仪协议校验 + 违规回滚）
- **可自举进化**：约束引擎产生的结构化数据可反哺模型微调（路线B），形成数据飞轮

---

## 二、名称说明

| 名称 | 说明 |
|------|------|
| **象 (Xiang)** | 项目总称，取自"象语言"——三套算子的统一约束语言 |
| **归藏 (GuiZang)** | 第一引擎 CangVM —— 执行控制器，周天算子循环 |
| **连山 (LianShan)** | 第二引擎 ShanVM —— 策略导航，障碍检测与方位决策 |
| **周易 (ZhouYi)** | 第三引擎 ZhouVM —— 认知姿态，八卦网格与温度调度 |
| **三易** | 三套引擎的统称 |
| **象语言 (XiangLang)** | 统一三套算子体系的 DSL（领域特定语言） |

---

## 三、系统架构

```
                    ┌──────────────────────────────┐
                    │   ShanVM (连山)               │
                    │   策略引擎 · 障碍导航          │
                    │   6步决策链: 艮→气→甲→元→峙→决 │
                    │   输出 7 种方位决策            │
                    └──────────┬───────────────────┘
                               │ 策略建议
                               ▼
┌─────────────────────────────────────────────────┐
│              CangVM (归藏)                        │
│          执行控制器 · 8算子周天                   │
│                                                   │
│  生→动→归→长→育→杀→止→藏                         │
│  决策: PASS / ROLLBACK / SKIP / STOP              │
│                                                   │
│  生成前: Gua状态机 → 偏离度 → 温度策略            │
│  生成中: XiangLogitBias → token概率调整           │
│  生成后: YinProtocolChecker → 格式验证 → 违规回滚 │
└──┬──────────────┬──────────────────┬─────────────┘
   │              │                  │
   ▼              ▼                  ▼
┌──────────┐  ┌──────────┐    ┌──────────┐
│ 阴仪协议  │  │ ZhouVM   │    │ CangSea  │
│ 正则校验  │  │ (周易)    │    │ 经验矩阵  │
│ 格式验证  │  │ 8卦网格   │    │ Hebbian  │
│ 偏离检测  │  │ 五行生克  │    │ 权重衰减  │
│          │  │ 温度调度  │    │ 语义存储  │
└──────────┘  └──────────┘    └──────────┘
```

### 执行流水线（单轮）

```
用户输入
   │
   ▼
┌─────────────────────────────┐
│ 1. CangVM 状态计算           │  ← 6-bit Gua → Hamming 偏离度
│    · 温度策略                │     偏离<0.3 → 0.8, >0.7 → 0.3
│    · 阶段算子选择（生/动/长/育）│
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ 2. LLM 生成（带 Logit-bias） │  ← XiangLogitBias
│    · 离焦token 压制 (-0.5~-8)│     连续强度缩放
│    · 发散token 轻度压制       │
│    · EOS 提升 (+0~+4)       │
│    · 偏离>0.95 强制截断      │
└─────────────┬───────────────┘
              │ 生成文本
              ▼
┌─────────────────────────────┐
│ 3. 阴仪协议校验              │  ← YinProtocolChecker
│    · 正则匹配算子规则         │     生/动/长/育 各4-8条正则
│    · 违规 → SHA+ROLLBACK    │
│    · 合规 → 追加到历史       │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│ 4. CangVM 经验沉淀           │  ← CangSea Hebbian 矩阵
│    · 成功/失败 → 权重更新    │
└─────────────────────────────┘
```

---

## 四、三引擎分工

| 引擎 | 名称 | 渊源 | 职责 | 实现状态 |
|:----:|:----:|:----:|------|:--------:|
| **CangVM** | 归藏 (GuiZang) | 《归藏易》 | 执行控制器：算子周天、偏离度计算、Logit-bias 生成、阴仪校验 | ✅ 完整实现 |
| **ShanVM** | 连山 (LianShan) | 《连山易》 | 策略导航：6步障碍检测链（艮→气→甲→元→峙→决），7种方位决策 | ✅ 代码就绪，未接入流水线 |
| **ZhouVM** | 周易 (ZhouYi) | 《周易》 | 认知姿态：8卦网格、五行生克权重、温度调度 | ✅ 代码就绪，未接入流水线 |

### 三引擎协同闭环

```
用户请求 → 周易选姿态 → 归藏执行周天 → 遇障碍 → 连山导航 → 归藏执行策略
                                                              ↓
                                                        藏海沉淀经验
                                                              ↓
                                                        经验回流各引擎
```

---

## 五、象语言：统一约束语言

**象语言不是归藏的语法糖。它是三套独立算子体系的统一约束语言。**

三套引擎共享同一套底层类型系统（定义在 `xiang-core` 中）：

| 共享类型 | 作用 | 被哪些引擎使用 |
|---------|------|--------------|
| **Gua(u8)** | 6 位卦象状态（0-63） | 全部三引擎 |
| **八气算子** | 生/动/归/长/育/杀/止/藏 | CangVM 执行，ShanVM 输出策略，ZhouVM 调整姿态 |
| **FangWei** | 7 种方位决策 | ShanVM 产出，CangVM 消费 |
| **Bagua** | 8 卦 + 生克权重矩阵 | ZhouVM 核心，CangVM 读取温度 |
| **CangSea** | 64×64 Hebbian 经验矩阵 + 语义存储 | 归藏沉淀，连山/周易消费 |

这种共享类型体系意味着：
1. **单条训练数据包含三层标签**：归藏的算子序列 + 偏离度、连山的方位决策 + 障碍等级、周易的卦象姿态 + 生克权重
2. **云端评判可以精确到每一层**：不仅评判"偏离度是否合理"，还评判"这个障碍下选择绕行是否合理"、"当前用震卦姿态是否合适"
3. **三层引擎各自积累经验**：归藏的藏海、连山的策略格栅、周易的转移权重，都可以从成功/失败频率中自调整

象语言语法示例：

```
定义 思维向导:
    卦 焦点状态 = 111111
    生
    动
    归
    若 偏离度 > 0.7:
        杀 110
        藏 恶
    若 偏离度 < 0.3:
        藏 善
    否则:
        止 101101
        藏 善
```

---

## 六、路线A：阶段约束（已实现）

### 实现范围

| 组件 | 文件 | 说明 |
|------|------|------|
| CangVM 核心 | `xiang-cangvm/src/vm.rs` | 8算子 + judge() + 三引擎注入接口（60+测试） |
| YinProtocolChecker | `xiang-core/src/yin_checker.rs` | 4算子正则规则集（11测试） |
| XiangLogitBias | `xiang-llm/src/lib.rs` | 连续强度缩放 Logit-bias（16测试） |
| MockBackend | `xiang-llm/src/lib.rs` | 5种合成模式 |
| HttpBackend | `xiang-llm/src/http_backend.rs` | 远程 llama.cpp 服务代理 |
| 基准测试框架 | `xiang-experiments/src/benchmark.rs` | 100组对比试验 + 统计分析 |
| 重复检测 | `xiang-experiments/src/lib.rs` | Trigram Jaccard 相似度早停 |
| 轮次文件记录 | `xiang-experiments/src/benchmark.rs` | 完整思考过程 + 干预手段记录 |
| 自举进化飞轮 | specs/ 设计 | 路线B 数据飞轮架构 |
| 235 测试全通过 | 全部 crate | 涵盖所有核心功能和边界条件 |

### 已知局限性（真实模型验证结论）

路线A通过 **外部程序化干预** 实现了约束，但以下问题在 Qwen3.5-4B 实机测试中暴露：

1. **Logit-bias 只能"堵"不能"教"** — 压制离焦/发散 token 后，模型不知道应该输出什么，导致生成量逐轮衰减
2. **正则校验只能"判"不能"纠"** — YinProtocolChecker 能检测违规，但无法指导模型生成合规内容
3. **外部约束不可泛化** — 每个新算子/新规则需手动编写对应的正则和 Logit-bias 配置
4. **生成速度受限** — 4B Q4_K_M 在 RX 6650 XT 上约 2.3 tok/s

**根本结论**：路线A证明了"可以在推理时约束 LLM"，但要真正实现"三易合规输出"，必须进入路线B——修改模型权重，内化约束原语。

### Mock 基准测试结果

| 指标 | 对照组 (裸跑) | 实验组 (三易控制) | 改善 | Cohen's d |
|:----|:----:|:----:|:----:|:----:|
| 偏离率 | 0.6351 | 0.2809 | **-55.8%** | 13.58 |
| 结构合规率 | 0.4040 | 0.8712 | **+115.7%** | 10.08 |
| 完成率 | 0.7850 | 1.0000 | **+27.4%** | 3.49 |

---

## 七、路线B：控制原语微调（设计完成，待实施）

### 设计方案

路线B 的核心创新：通过 **词表扩展 + 控制原语嵌入 + LoRA 微调**，将八气算子从"外部约束协议"内化为模型自身的控制原语。

```
┌──────────────────────────────────────────────┐
│  CangVM (硬逻辑)                              │
│  驱动算子流转：拼接控制原语序列                │
│  [生][动][归][长][育][杀][止][藏]              │
└──────────────────┬───────────────────────────┘
                   │ 控制原语 token 序列
                   ▼
┌──────────────────────────────────────────────┐
│  阳仪 (微调后模型)                             │
│  · 14 控制原语 token（Unicode 私有区）         │
│  · LoRA 微调内化约束                           │
│  · 零 prompt 生成合规输出                      │
└──────────────────┬───────────────────────────┘
                   │ token 流
                   ▼
┌──────────────────────────────────────────────┐
│  阴仪 (正则检查 + 原语监听)                    │
│  · 检测 _end 原语 → 提取算子文本               │
│  · 语义偏离度（MockEncoder）                   │
└──────────────────────────────────────────────┘
```

详细设计见：[specs/02-路线B-控制原语微调.md](./specs/02-路线B-控制原语微调.md)

### 阻塞原因：硬件与软件栈限制

**路线B目前无法在当前开发硬件上实施。**

#### 硬件层面

| 需求 | 当前硬件 | 结论 |
|------|---------|:----:|
| 4B QLoRA 微调 VRAM | RX 6650 XT (8GB) | ⚠️ 勉强可行（~7GB），无余量 |
| 4B 全量微调 VRAM | 需要 ≥16GB | ❌ 不可行 |
| 训练批处理 VRAM | 需要 ≥12GB | ❌ 不可行 |

#### 软件层面

| 方案 | Windows AMD 支持 | 说明 |
|:----:|:----------------:|:----:|
| bitsandbytes | ❌ 不支持 | 仅 CUDA，AMD GPU 无可用量化训练库 |
| Unsloth | ⚠️ WSL 有限 | RDNA2 (gfx1032) 需额外 ROCm 配置 |
| Hugging Face PEFT | ✅ CPU 支持 | CPU 训练极慢（~0.1 tok/s） |
| llama.cpp 训练 | ⚠️ 实验性 | 不支持 LoRA，功能有限 |
| WSL2 + ROCm | ⚠️ 不稳定 | RDNA2 驱动支持不成熟 |

**结论**：RX 6650 XT (8GB) + Windows 环境下，暂无可用 QLoRA 训练工具链。

### 推荐解除阻塞路径

1. **短期**：云端 GPU（Google Colab / AutoDL / Lambda Labs），预估训练时间 3-6 小时/模型
2. **中期**：硬件升级至 16GB+ VRAM（RTX 4060 Ti 16GB / RX 7800 XT）
3. **长期**：ROCm 对 RDNA2 完整支持，或迁移至 Linux 环境

---

## 八、路线C：太极双LLM（远期规划）

路线C 引入双 LLM 架构——专注生成的 **阳仪** 和专注评估的 **阴仪**。

- **阳仪**：负责内容生成（当前 Qwen3.5-4B）
- **阴仪**：负责语义偏离度计算、障碍检测、策略评估（轻量编码器或小模型）
- **藏海v2**：语义级经验检索，替代路线A 的 Hamming 偏离度

详细设计见 [specs/03-路线C-太极双LLM.md](./specs/03-路线C-太极双LLM.md)

---

## 九、硬件约束与当前瓶颈

### 开发环境

| 组件 | 规格 |
|------|------|
| CPU | Intel i5-10600KF |
| GPU | AMD RX 6650 XT (8GB VRAM, RDNA2/gfx1032) |
| 内存 | 48GB DDR4 |
| 操作系统 | Windows 10 / WSL 混合 |
| 模型 | Qwen3.5-4B-Uncensored (Q4_K_M, ~2.5GB) |
| 推理框架 | llama.cpp (Vulkan, 64K 上下文) |

### 推理性能

| 操作 | 性能 | 说明 |
|------|:----:|------|
| 4B Q4_K_M 推理 (GPU) | ~2.3 tok/s | RX 6650 XT 单 batch |
| Vocab 扫描 (147+26 tokens) | ~3-5s | 一次性，缓存 |
| 首次冷生成 (80 tokens) | ~30-35s | cache_prompt 后降至 ~20-25s |

### 训练瓶颈（路线B）

| 项目 | 状态 | 说明 |
|:----:|:----:|------|
| 4B QLoRA (本地 GPU) | ❌ 不可行 | AMD Windows 无可用训练工具链 |
| 4B QLoRA (云端) | ✅ 可行 | Google Colab/AutoDL ~3-4h |
| 0.5B LoRA (本地) | ⚠️ 可能 | 模型太小，训练数据质量不足 |
| 训练数据导出 (CangSea) | 📋 待实现 | 藏海 → JSONL pipeline 未完成 |

---

## 十、技术栈

### 后端（Rust）

| 技术 | 用途 |
|------|------|
| Rust 2024 edition | 全部 9 个 crate |
| axum + tokio | HTTP 服务 (xiang-chat) |
| serde / serde_json | 序列化 |
| ureq | HTTP 客户端 (HttpBackend) |
| llama.cpp (Vulkan) | GGUF 模型推理 |
| rand | 随机数生成 (ZhouVM Markov) |
| uuid | 唯一标识 (CangSea) |

### 前端

| 技术 | 用途 |
|------|------|
| React 18 + TypeScript | UI |
| Vite 6 | 构建工具 |
| Tailwind CSS 3.4 | 样式 |

### 约束引擎核心

| Crate | 行数(约) | 测试数 | 职责 |
|-------|:--------:|:------:|------|
| xiang-core | ~2000 | 77 | Gua/Bagua/偏离度/CangSea/阴仪协议 |
| xiang-parser | ~1000 | 12 | 象语言 词法/语法/AST |
| xiang-cangvm | ~1500 | 60 | CangVM 执行引擎 + 三引擎编排 |
| xiang-shanvm | ~400 | 17 | 连山 6步策略链 |
| xiang-zhouvm | ~300 | 14 | 周易 8卦网格 |
| xiang-verifier | ~500 | 14 | 可信验证 |
| xiang-llm | ~700 | 16 | Logit-bias/LLM 后端抽象 |
| xiang-experiments | ~2000 | 33 | 实验框架/基准测试 |
| xiang-chat | ~1500 | 2 | Axum 聊天服务 |

---

## 十一、Crate 依赖关系

```
xiang-chat
  ├── xiang-cangvm
  │    ├── xiang-core
  │    ├── xiang-parser
  │    ├── xiang-shanvm
  │    └── xiang-zhouvm
  ├── xiang-llm
  ├── xiang-shanvm
  └── xiang-core

xiang-experiments (CLI)
  ├── xiang-cangvm
  ├── xiang-llm
  └── xiang-core

xiang-verifier
  ├── xiang-core
  └── xiang-parser
```

---

## 十二、贡献指南

### 最需要的贡献

| 领域 | 说明 | 难度 |
|------|------|:----:|
| **路线B 训练 pipeline** | 构建 QLoRA 微调脚本（云端优先） | ⭐⭐⭐ |
| **AMD GPU 训练支持** | 解决 bitsandbytes/ROCm 兼容性 | ⭐⭐⭐⭐ |
| **ShanVM 流水线接入** | 将连山策略引擎接入 CangVM 主循环 | ⭐⭐ |
| **ZhouVM 流水线接入** | 将周易姿态引擎接入 CangVM 主循环 | ⭐⭐ |
| **CangSea 数据导出** | 实现藏海训练数据导出为 JSONL | ⭐⭐ |
| **新算子规则** | 为 YinProtocolChecker 添加更多正则规则 | ⭐ |
| **前端增强** | chat-ui 交互优化、可视化 | ⭐ |

### 开发环境要求

- Rust 2024 edition (nightly)
- Node.js 18+（前端）
- llama.cpp (Vulkan)：`-DGGML_VULKAN=ON`
- 模型文件：任意 GGUF 格式（推荐 Qwen 系列）
- 最低硬件：4GB VRAM + 16GB 内存

### 本地快速开始

```bash
# 构建
cd xing && cargo build

# 启动全部服务
./start_all.bat

# 运行 Mock 基准测试
cargo run --bin xiang-exp -- bench --mock offfocus --seed 42

# 运行真实模型基准测试
cargo run --bin xiang-exp --features http_backend -- bench --http http://localhost:8080

# 运行全部测试
cargo test
```

---

> **象 (Xiang)** — 三易智能约束系统，基于象语言。归藏、连山、周易，三套易经算子体系统一于同一框架。
>
> *归藏是执行者，连山是导航者，周易是姿态者。三者合一，构成完整的认知约束闭环。*
>
> 技术规格见 [specs/](./specs/) 目录。
