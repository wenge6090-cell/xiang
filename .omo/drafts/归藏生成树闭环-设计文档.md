# 归藏生成树闭环 — 观测层、干预层、管理层设计

> 版本：1.0 | 生成日期：2026-05-27
> 
> 本文档定义归藏引擎从"纯位运算状态机"升级为"完整的认知约束闭环"所需的所有设计决策。
> 以第一性原理（specs/10-第一性原理审视.md）为锚点，以代码实际断点为驱动。

---

## 〇、核心命题

**三易不是消灭幻觉——三易是在给涌现提供轨道。**

LLM 的幻觉与涌现同源，都来自连续向量空间的统计生成。杀死幻觉的方式（RLHF 过度压制、Logit-Bias 过分激进）必然同时杀死涌现。三易不压制模型的内容选择——它在周围搭一个可变的"栅栏"：

| 策略 | 做法 | 对涌现的影响 |
|:----|:----|:----------|
| 压制式（主流） | Token 概率打到 -∞ | 模型变弱，不敢创造 |
| 引导式（三易） | 改变方向重心 ±1.0 | 模型在方向内自由涌现 |

---

## 一、三层执行架构（修正版）

### 1.1 整体视图

```
用户输入（锚点：问题焦点）
    │
    ▼
┌─────────────────────────────────────────────────┐
│           周易 (ZhouVM) — 注意力姿态层             │
│                                                    │
│  功能：设定注意力的"工作模式"                       │
│  对应 LLM 内建机制：标准 Self-Attention（全连接）    │
│                                                    │
│  乾(创造)→温度1.2→注意力更敢于远距离跳转             │
│  离(分析)→温度0.5→注意力更精确收敛                  │
│  艮(审慎)→温度0.3→注意力最集中，不易漂移             │
│                                                    │
│  它不计算注意力——它改变注意力的"行为风格"             │
└──────────────────────┬──────────────────────────┘
                       │ 温度 + 姿态
                       ▼
┌─────────────────────────────────────────────────┐
│       归藏 (CangVM) — 注意力阶段干预层             │
│                                                    │
│  功能：标记当前认知阶段，实时干预 token 概率分布     │
│  对应 LLM 外建机制：带算子方向引导的 Logit-Bias     │
│                                                    │
│  生（萌发）→偏置重心偏向探索性 token池              │
│  动（发散）→偏置重心偏向多角度 token池              │
│  长（聚焦）→偏置重心偏向收敛性 token池              │
│  育（构建）→偏置重心偏向结构性 token池              │
│                                                    │
│  干预强度：±1.0 以内（方向引导，非内容压制）         │
│  它不压制"错误 token"——它改变 token 空间的采样重心   │
└──────────────────────┬──────────────────────────┘
                       │ 偏离度信号
                       ▼
┌─────────────────────────────────────────────────┐
│       连山 (ShanVM) — 注意力策略导航层             │
│                                                    │
│  功能：遇到障碍时切换注意力策略路径                 │
│  对应 LLM 类比：稀疏注意力（选择性关注方向）         │
│                                                    │
│  硬推(PushThrough)     → 保持当前注意力方向不变     │
│  绕行(NavigateAround)  → 转移注意力到替代路径      │
│  分解(Decompose)       → 切分注意力为子任务        │
│  升级(Escalate)        → 扩展注意力范围            │
│  放弃(Abort)           → 终止当前注意力路径        │
│  等待(WaitGather)      → 收缩注意力，等待信息      │
│                                                    │
│  它不计算注意力——它决定注意力该往哪个策略方向走      │
└─────────────────────────────────────────────────┘
```

### 1.2 三层如何协同防目标漂移

```
token 级漂移：
  → 周易：降低温度，减少随机性
  → 归藏：压制偏离方向 token（±0.5 bias）
  → 连山：不激活（偏离度 < 0.5，无需策略改变）

句子级漂移：
  → 周易：切换到收敛/分析姿态
  → 归藏：归算子观测到偏离度持续上升（0.3→0.5→0.7）
  → 连山：激活策略，决定绕行或分解
  → 杀：如果未恢复，裁剪该轮对话历史

段落级漂移：
  → 连山：持续决策失败 → 升级
  → 归藏：high deviation + 多轮 kill → 藏
  → 执行层：清空对话上下文，系统重置
```

### 1.3 三层与 LLM 注意力的映射关系

| LLM 内部（连续，不可读） | 三易（离散，可读可验证） | 工具 |
|:----------------------|:---------------------|:----|
| Self-Attention 全连接注意力 | 周易——注意力姿态层 | ZhouVM 八卦网格 |
| 注意力阶段隐式切换 | 归藏——注意力阶段标签 | CangVM 生成树 |
| 注意力策略隐式调整 | 连山——注意力策略导航 | ShanVM 六步决策链 |

**三层不替代注意力——三层是注意力的白箱化接口。**

---

## 二、幻觉与涌现：三易的边界

### 2.1 项目不试图解决的问题

```
❌ 模型输出的统计数据是否真实
❌ 模型引用的文献是否确实存在
❌ 模型做的推理逻辑在外部事实层面是否正确
```

**三易不知道外部事实。** 它的约束基于形式（卦象、偏离度、格式规则），不基于内容真实性。

### 2.2 项目实际解决的问题

```
✅ 模型是否在正确的算子阶段生成（形式约束）
✅ 模型的输出格式是否符合当前阶段要求（阴仪协议）
✅ 模型是否偏离了用户初始意图（三层注意力协同）
✅ 模型如果偏离，上下文是否被正确裁剪（杀/止/藏）
✅ 系统的每一步内部状态对外部观察者是否可读可审计（白箱化）
```

### 2.3 当前防御幻觉的策略

| 防御层 | 手段 | 覆盖范围 |
|:------|:----|:--------|
| 算子阶段引导 | Logit-Bias 方向引导 | token 级——引导而非压制 |
| HanziMap 观测 | 语义断裂检测 | 句子级——发现语义跳跃 |
| 上下文裁剪 | 杀算子物理删除偏离内容 | 段落级——不留下一次污染 |
| 周天循环 | 藏算子清空上下文重新开始 | 全局级——重置认知起点 |

### 2.4 路线C 的语义验证（远期）

路线C（太极双LLM）引入独立阴 LLM 做事实校验——这是项目计划中唯一直接对抗幻觉的机制。但前提是双模型硬件条件（16GB+ VRAM）或外部 API。

---

## 三、观测层：归 + HanziMap

### 3.1 设计原则

> 观测任何时候都接上。干预只做方向引导，不做内容压制。执行只处理已发生的偏离，不预防未发生的。

**归是八气算子中唯一不做功的算子**。它不修改 S，只测量偏离度。它是意识的自反性——"看自己流到哪里了"。

### 3.2 两种偏离度

| 偏离度 | 计算方式 | 范围 | 已实现 |
|:------|:--------|:----|:------|
| 形式偏离度 | Hamming(state, origin) / 6.0 | [0, 1] | ✅ |
| 语义偏离度 | hybrid: alpha·Hamming + (1-alpha)·cosine(Hanzi投影指纹) | [0, 1] | ❌ (待接线) |

### 3.3 EmbeddingObserver 管道

```
LLM 解码每一步
    ↓
输出嵌入 [n_embd 维]
    ↓
HanziMap.map_top_k(emb, K) → 汉字序列 ["探","索","发","现"]
    ↓
EmbeddingObserver.observe(emb)
    ├── → 语义指纹序列 [汉字...]
    ├── → 与锚点的余弦偏离度
    └── → 五行元素分布
           ↓
CangVM.set_semantic_deviation() → hybrid_deviation() → judge()
```

### 3.4 观测 vs 干预的分离

| | 归 + HanziMap | 生/动/长/育 (Logit-Bias) | 杀/止/藏 |
|:--|:-------------|:------------------------|:--------|
| 时间 | 每步生成后 | 每步生成前 | 整轮生成后 |
| 操作 | 只记录，不修改 | 改 token 概率分布 | 改对话历史/KV cache |
| 对模型影响 | 无 | ±1.0 方向引导 | 裁剪已偏离内容 |

**观测层必须最先接上。** 因为如果不观测到偏离，干预和执行就没有启动信号。

---

## 四、干预层：生/动/长/育 算子差异化 Logit-Bias

### 4.1 当前状态

当前 `XiangLogitBias` 对所有算子使用同一套 token 压制策略（off_focus / divergent）。这丢失了算子阶段的差异化引导能力。

### 4.2 算子专属 token 池

每个算子定义自己的"方向引导池"——一组 token id，在每个算子阶段获得不同的偏置方向：

| 算子 | 阶段语义 | 期望行为 | 偏置策略 |
|:----|:--------|:--------|:--------|
| **生** | 探索·提问·起始 | 模型应扩展思路、提出新方向 | Positive: 探索性 token（也许/可能/方向/假设）<br>Negative: 结论性 token（因此/总之/证明） |
| **动** | 扩展·连接·发散 | 模型应从多角度关联 | Positive: 发散性 token（但是/然而/另一方面）<br>Negative: 重复性 token（也就是说/即） |
| **长** | 深入·聚焦·收敛 | 模型应深入分析核心 | Positive: 收敛性 token（因此/这表明/因为…所以）<br>Negative: 新方向 token（另外/此外/换个角度） |
| **育** | 构建·结构化·规划 | 模型应组织为结构化输出 | Positive: 结构性 token（第一步/其次/综上所述）<br>Negative: 模糊性 token（某种/也许/大概） |

### 4.3 偏置强度规则

```
基础强度: ±0.5（所有算子默认）
偏离度调节: ±0.3 × deviation（偏离时适当加强方向引导）
连山策略调节: ±0.2 × strategy_factor（策略切换时微调）

最大强度上限: ±1.0（硬上限——防止过度压制杀死涌现）
```

### 4.4 与连山策略的协同

当连山决策为 `PushThrough` 时：
- 所有算子的 positive bias 增加 0.2（鼓励继续当前方向）

当连山决策为 `NavigateAround` 时：
- 所有算子的 negative bias 增加 0.2（加强偏离抑制）

**注意：连山只调节 bias 强度，不改变 token 池的构成。**

---

## 五、管理层：杀/止/藏 上下文操作

### 5.1 设计原则

> 杀/止/藏是压缩树，只做上下文管理，不做语义判断。

**压缩树 = 纯粹的上下文管理器。** 不生成新的认知内容，不改变 Gua 状态之外的东西，不参与 LLM 的推理过程。

### 5.2 三层上下文管理

#### 第一层：对话历史管理（即刻可做）

| 算子 | 触发条件 | 操作 |
|:----|:--------|:----|
| **杀** | 偏离度 > 0.7 且持续多轮未恢复 | 删除最近 1-2 轮对话历史 |
| **止** | 偏离度 > 0.5 但未触发杀 | 注入"冻结标记"，系统提示词追加"请基于已有信息做结论" |
| **藏** | 周天循环完成 | 提取本周期摘要保留，清空消息列表 |

CangVM 新增信号输出：

```rust
pub enum MetabolismSignal {
    /// 杀 N 轮：裁剪最近 N 轮对话历史
    Sha(u32),
    /// 止：冻结上下文（不再追加新信息）
    Zhi,
    /// 藏：清空上下文（保留技术摘要）
    Cang {
        summary: String,     // 本轮代谢摘要
    },
}
```

#### 第二层：KV Cache 管理（需要 llama.cpp）

基于 seq_id 的 KV cache 操作：

```
每轮生成使用独占的 seq_id
  → kv_cache_snapshot() 保存当前 seq_id
  → 如果 judge() 返回 Rollback → 删除当前 seq_id 对应的所有 KV cache 条目
  → 下一轮生成从快照状态开始

对于 HttpBackend（远程 API）：无法利用 seq_id
  → 回退方案：通过重新打包提示词实现"软回滚"
      将偏离轮次从 history 中移除，重新构建完整 prompt
  → 比 KV cache rollback 代价高（需要重新计算前序 KV cache）
  → 但对于仅 ~2.3 tok/s 的推理速度，此开销在可接受范围内
```

#### 第三层：Gua → 上下文操作的映射

在 `CangVM::execute_phase_operator()` 中新增信号输出：

```rust
"杀" => {
    self.state = self.state.sha();
    self.sha_count += 1;
    // 新增：发出上下文裁剪信号
    self.metabolism_signal = Some(MetabolismSignal::Sha(2));
}
"止" => {
    self.state = self.state.zhi();
    // 新增：发出冻结信号
    self.metabolism_signal = Some(MetabolismSignal::Zhi);
}
"藏" => {
    // ... 现有藏海存储逻辑 ...
    // 新增：发出清空信号
    let summary = self.metabolism.section();
    self.metabolism_signal = Some(MetabolismSignal::Cang { summary });
    self.state = self.state.cang();
}
```

然后 `ConstrainedEngine::generate()` 的主循环在 judge() 之后检查信号并执行实际操作。

### 5.3 信号处理流程

```
CangVM::execute_phase_operator()
    ↓
metabolism_signal 被设置
    ↓
回到 ConstrainedEngine::generate()
    ↓
match signal {
    Sha(n) → 从 self.messages 和 ctx.history 删除最后 n 轮
    Zhi   → 设置 meta.frozen = true（系统提示词追加冻结指令）
    Cang{summary} → 保留 summary，清空 messages 和 history
}
```

---

## 六、接线总图

### 6.1 数据流

```
┌──────────────────────────────────────────────────────────────────┐
│                     ConstrainedEngine::generate()                   │
│                                                                      │
│  1. ZhouVM: select_zhou_pose() → temperature                        │
│                                                                      │
│  2. CangVM: current_operator() → "生"/"动"/"长"/"育"                │
│                                                                      │
│  3. CangVM: execute_phase_operator(&operator) → Gua 切换            │
│     └── 可能设置 metabolism_signal                                  │
│                                                                      │
│  4. CangVM: current_deviation() → hybrid_deviation                   │
│     ├── Hamming deviation (always available)                         │
│     └── Semantic deviation (if EmbeddingObserver is injected)        │
│                                                                      │
│  5. deviation > 0.5 ? → ShanVM obstacle navigation                  │
│                                                                      │
│  6. XiangLogitBias: 算子差异化 bias 构造                             │
│     ├── off_focus tokens (common)                                    │
│     ├── divergent tokens (common)                                    │
│     ├── operator_positive tokens (算子专属)                          │
│     └── operator_negative tokens (算子专属)                          │
│                                                                      │
│  7. LLM: generate_with_logit_bias → 生成文本 + 嵌入向量              │
│                                                                      │
│  8. EmbeddingObserver.observe(embedding) → 汉字轨迹 + 语义偏离度      │
│     └── set_semantic_deviation() → 影响下一轮的 hybrid_deviation     │
│                                                                      │
│  9. Analyzer: compute_output_deviation(text)                         │
│                                                                      │
│ 10. YinProtocolChecker: check_operator_output()                      │
│                                                                      │
│ 11. judge() → Pass / Rollback / Skip / Stop / FinishCycle            │
│                                                                      │
│ 12. 检查 metabolism_signal → 执行上下文操作                           │
│     ├── Sha → crop history                                           │
│     ├── Zhi → freeze                                                 │
│     └── Cang → clear + restart                                       │
│                                                                      │
│ 13. 周天完成 ? 进入下一轮 : 生成结束                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 6.2 组件接线状态

| 组件 | 当前状态 | 目标状态 | 优先级 |
|:----|:--------|:--------|:------|
| EmbeddingObserver | 代码就绪，未注入 | 在 ConstrainedEngine::new() 中注入 | **P0** |
| HanziMap | 代码就绪，HanziMap::empty() 降级可用 | 优先编译时嵌入 | **P0** |
| 算子差异化 Logit-Bias | 仅 off_focus/divergent 通用池 | 每个算子专属 token 池 | P1 |
| MetabolismSignal | 不存在 | CangVM 新增枚举 + 输出字段 | P1 |
| 对话历史裁剪 | 不存在 | ConstrainedEngine 处理 Sha/Zhi/Cang | P1 |
| KV cache snapshot | 占位返回 -1 | 基于 seq_id 实现 | P2 |
| KV cache rollback | 空函数 | 基于 seq_id 实现 | P2 |
| 实机验证 | 无 | 用 Qwen3.5-4B 跑完整闭环 | P3 |

---

## 七、代码修改清单

### 7.1 P0 — 观测层接线

| 文件 | 修改 |
|:----|:----|
| `crates/xiang-chat/src/main.rs` | `ConstrainedEngine::new()` 中创建 `HanziMap` 并调用 `vm.with_embedding_observer()` |
| `crates/xiang-chat/src/main.rs` | `generate()` 的第 315-324 行确认 embedding 返回路径 |
| `crates/xiang-core/src/hanzi_map.rs` | 确认 `from_embedded_bytes` 的二进制格式从 `export_hanzi_embeddings.py` 输出获取 |

### 7.2 P1 — 算子差异化 Logit-Bias + 上下文管理

| 文件 | 修改 |
|:----|:----|
| `crates/xiang-llm/src/lib.rs` | `XiangLogitBias` 新增 `operator: &'static str` 字段和算子专属 token 池 |
| `crates/xiang-llm/src/lib.rs` | `bias_for_step()` 根据 `operator` 添加 Positive/Negative 偏置规则 |
| `crates/xiang-cangvm/src/vm.rs` | 新增 `MetabolismSignal` 枚举和 `metabolism_signal: Option<MetabolismSignal>` 字段 |
| `crates/xiang-cangvm/src/vm.rs` | `execute_phase_operator()` 的杀/止/藏分支设置信号 |
| `crates/xiang-chat/src/main.rs` | `generate()` 的 judge() 后检查信号并执行上下文操作 |
| `crates/xiang-chat/src/main.rs` | `LlmContext` 新增 `crop_last_n(n)` 和 `freeze()` 方法 |

### 7.3 P2 — KV cache 管理

| 文件 | 修改 |
|:----|:----|
| `crates/xiang-llm/src/llama_backend.rs` | 实现 `kv_cache_snapshot()` —— 基于 seq_id |
| `crates/xiang-llm/src/llama_backend.rs` | 实现 `kv_cache_rollback(seq_id)` —— 删除指定 seq_id 的 KV cache |
| `crates/xiang-llm/src/http_backend.rs` | HttpBackend 回退方案：提示词重打包 |

---

## 八、设计约束

### 8.1 不可违反原则

1. **Logit-Bias 强度上限 ±1.0**——防止过度压制杀死涌现
2. **杀/止/藏只操作已发生的偏离**——不预防未发生的内容
3. **归观测任何时候都接上**——即使 HanziMap 不可用（降级为纯 Hamming）
4. **观测结果不作为惩罚依据**——汉字序列只用于人类可读和白箱审计
5. **三引擎各自可独立测试**——不因集成导致测试失效

### 8.2 与第一性原理的对齐

| 第一性原理要求 | 本设计如何满足 |
|:-------------|:------------|
| 内部状态是人类可读的离散符号 | HanziMap 将连续嵌入投影为汉字序列（观测层） |
| 状态转移是确定性的、可追溯的 | Gua 位运算 + MetabolismSignal 枚举 |
| 思考与表达分离 | 三引擎思考 → LLM 表达（约束下的文本生成） |
| 外部约束是渐进可逼近的 | P0→P1→P2 分阶接线，每步可验证 |
| 底层模型不需要变白箱——只需要被有效约束 | Logit-Bias 方向引导 + 上下文管理，不改模型权重 |

---

> **本文档是归藏引擎从"位运算状态机"升级为"完整认知约束闭环"的设计锚点。**
>
> 观测优先（归+HanziMap）→ 引导其次（生/动/长/育）→ 清理最后（杀/止/藏）。
>
> **不压制，只引导。不预防，只清理。不假设，只观测。**
